# This file is created and managed by `unidep` 0.66.0.
# For details see https://github.com/basnijholt/unidep
# File generated with: `unidep merge --name myenv --verbose`

name: myenv
channels:
  - conda-forge
dependencies:
  - sel(linux): adaptive-scheduler
  - numpy >=1.21
  - sel(linux): hpc05
  - pandas >=1,<3
  - sel(linux): pexpect
  - sel(osx): pexpect
  - sel(linux): adaptive >=0.15.0, <2.0.0
  - sel(linux): pfapack
  - packaging
  - pytest
  - pytest-cov
  - pip:
    - unidep; sys_platform == 'linux' and platform_machine == 'x86_64'
    - unidep; sys_platform == 'darwin'
    - markdown-code-runner
    - numthreads
    - yaml2bib; sys_platform == 'linux' and platform_machine == 'x86_64'
    - rsync-time-machine
    - slurm-usage
    - fileup; sys_platform == 'darwin'
    - pyyaml
    - aiokef
platforms:
  - linux-64
  - osx-64
  - osx-arm64
