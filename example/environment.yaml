# This file is created and managed by `unidep` 0.14.0.
# For details see https://github.com/basnijholt/unidep
# File generated with: `unidep merge`

name: myenv
channels:
  - conda-forge
dependencies:
  - pandas
  - scipy
  - pip:
    - qsimcirq
    - adaptive
