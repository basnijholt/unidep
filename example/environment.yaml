# This file is created and managed by `unidep` 0.55.0.
# For details see https://github.com/basnijholt/unidep
# File generated with: `unidep merge --name myenv --verbose`

name: myenv
channels:
  - conda-forge
dependencies:
  - sel(linux): adaptive-scheduler
  - numpy >=1.21
  - sel(linux): hpc05
  - pandas >=1,<3
  - sel(linux): pexpect
  - sel(osx): pexpect
  - adaptive
  - sel(linux): pfapack
  - packaging
  - pip:
    - unidep
    - markdown-code-runner
    - numthreads
    - yaml2bib; sys_platform == 'linux' and platform_machine == 'x86_64'
    - rsync-time-machine
    - slurm-usage
    - fileup; sys_platform == 'darwin'
    - pyyaml
    - aiokef
platforms:
  - linux-64
  - osx-64
  - osx-arm64
