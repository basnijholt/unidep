# This file is created and managed by `unidep` 0.29.0.
# For details see https://github.com/basnijholt/unidep
# File generated with: `unidep merge --name myenv --verbose`

name: myenv
channels:
  - conda-forge
dependencies:
  - pandas
  - adaptive >=1.0.0, <2.0.0
  - pfapack
  - pipefunc
  - adaptive-scheduler
  - hpc05
  - pip:
    - yaml2bib; sys_platform == 'linux' and platform_machine == 'x86_64'
    - rsync-time-machine
    - slurm-usage
    - fileup; sys_platform == 'darwin'
    - codestructure
    - aiokef
    - markdown-code-runner
    - home-assistant-streamdeck-yaml
    - unidep
platforms:
  - linux-64
