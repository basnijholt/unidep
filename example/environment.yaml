# This file is created and managed by `unidep` 0.32.1.
# For details see https://github.com/basnijholt/unidep
# File generated with: `unidep merge --name myenv --verbose`

name: myenv
channels:
  - conda-forge
dependencies:
  - pandas
  - adaptive >=1.0.0, <2.0.0
  - pfapack
  - pipefunc
  - adaptive-scheduler
  - hpc05
  - pip:
    - yaml2bib
    - rsync-time-machine
    - slurm-usage
    - codestructure
    - aiokef
    - markdown-code-runner
    - home-assistant-streamdeck-yaml
    - unidep
platforms:
  - linux-64
