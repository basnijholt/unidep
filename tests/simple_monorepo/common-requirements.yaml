# This file is uses in the `includes:` section in `project1/requirements.yml`
# and `project2/requirements.yml`.
name: common-requirements
channels:
  - conda-forge
dependencies:
  - conda: python_abi
